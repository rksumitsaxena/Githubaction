name: my workflow
on: push
jobs:
  first_job:
    runs_on: ubuntu-latest
    steps:
    - name: welcome mssg
      run: echo "hello sumit"
    - name: list file
      run: list
    - name: create file
      run: cat myfile.tf