name: my workflow
on: push
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
    - name: welcome mssg
      run: echo "hello sumit"

    - name: list file
      run: ls

    - name: create file
      run: cat myfile.tf